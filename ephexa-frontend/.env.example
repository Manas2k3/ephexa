# Environment Variables for SafeChat Frontend

# API Server URL (same origin in production since served from same server)
VITE_API_URL=http://localhost:3001

# Socket.IO Server URL (same origin in production)
VITE_SOCKET_URL=http://localhost:3001

# Google OAuth Client ID (for Google Sign-In)
VITE_GOOGLE_CLIENT_ID=your-google-client-id-here

# Production Note:
# When deployed to Railway, set these to:
# VITE_API_URL=https://ephexa.me
# VITE_SOCKET_URL=https://ephexa.me
